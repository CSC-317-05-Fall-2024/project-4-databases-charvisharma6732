<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= restaurant.name %> - Details</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <!-- Header and Navbar can go here if needed -->

    <main>
        <h1><%= restaurant.name %></h1>
        <img src="<%= restaurant.photo %>" alt="<%= restaurant.name %>" style="width:300px; height:auto; border-radius: 8px;">
        <p><strong>Address:</strong> <%= restaurant.address %></p>
        <p><strong>Phone:</strong> <%= restaurant.phone %></p>

        <h2>Reviews</h2>
        <section class="reviews">
            <% if (reviews && reviews.length > 0) { %>
                <% reviews.forEach(review => { %>
                    <div class="review">
                        <p><strong>Rating:</strong> <%= review.rating %> / 5</p>
                        <p><strong>Review:</strong> <%= review.content %></p>
                    </div>
                <% }) %>
            <% } else { %>
                <p>No reviews available for this restaurant.</p>
            <% } %>
        </section>
    </main>

    <!-- Footer can go here if needed -->

</body>
</html>